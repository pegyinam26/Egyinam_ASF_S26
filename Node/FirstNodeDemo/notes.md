1) create parent directory
2) npm init [-y] (creates package.json. This is the blueprint for our app, everything we need to create the app)
3) create index.ts
4) install packages => we will use eventually (npm i cat-me)
5) import the package (s)